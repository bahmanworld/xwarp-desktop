name: Publish Release

on: push

jobs:
  build_and_release:
    runs-on: macos-latest

    # strategy:
    #   matrix:
    #     os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x

      - name: install dependencies
        run: npm ci

      - name: MacOS Build App
        run: npm run release:macos
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      # Upload binaries to release
      # - name: Upload Release Asset
      #   uses: svenstaro/upload-release-action@v2
      #   with:
      #       repo_token: ${{ secrets.GH_TOKEN }}
      #       # file: ${{github.workspace}}/${{ steps.set_artifact_filename.outputs.filename }}
      #       file: ./build/1.0.2/${{ steps.set_artifact_filename.outputs.filename }}
      #       asset_name: ${{ steps.set_artifact_filename.outputs.appName }}
      #       tag: ${{ github.ref }}
      #       overwrite: true
      #       file_glob: true
